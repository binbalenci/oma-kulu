/**
 * Transactions Tab - Main entry point
 *
 * Thin wrapper that imports the Transactions feature screen.
 * This keeps the tab routing clean while allowing the feature
 * to be developed independently.
 */

import { TransactionsScreen } from "@/app/features/transactions";

export default TransactionsScreen;
