/**
 * Budget Tab - Main entry point
 *
 * Thin wrapper that imports the Budget feature screen.
 * This keeps the tab routing clean while allowing the feature
 * to be developed independently.
 */

import { BudgetScreen } from "@/src/features/budget";

export default BudgetScreen;
